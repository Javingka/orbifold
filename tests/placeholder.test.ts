// SPDX-License-Identifier: AGPL-3.0-only
// Placeholder test — ensures Vitest finds at least one test file before
// Phase 00 step 00.4 adds real engine tests. Remove once real tests exist.
import { describe, it, expect } from 'vitest';

describe('placeholder', () => {
  it('passes trivially', () => {
    expect(true).toBe(true);
  });
});
